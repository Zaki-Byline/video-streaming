# Production environment - uses relative /api path so requests stay on the same domain
# This prevents CORS issues when frontend and backend are served from the same host
VITE_API_URL=https://qr-test.kodeit.digital/api
