# Frontend environment variables
# Copy this file to .env (development) or .env.production (production)

# API URL — set to your backend API base URL
# Development (local): leave unset or use relative path
# Production (same domain): use /api  ← prevents CORS issues
# Production (different domain): use https://your-api-domain.com/api
VITE_API_URL=/api
